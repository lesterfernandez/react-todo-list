import styled from "styled-components";

const App = styled.div`
  min-height: 100vh;
  height: 100%;
  min-width: 100vw;
  background-image: linear-gradient(45deg, #fae, #297);
  display: grid;
  grid-template-rows: 45vh auto;
  gap: 3rem;
  justify-items: center;
  align-items: center;
`;

export default App;
